#include <stdio.h>
#include<stdlib.h>

typedef struct list
{
 int value;
}List;
int main(int argc, char *argv[]) 
{
    char str[100];
    int i;
    struct list List;
    while(fgets(str, sizeof(str), stdin)){
        list.value=str;
      if(str==-1)
      {
          for(i=0;i<100;i++;)
          {printf("%s,", list.value);}
          break;
        
    }

    return 0;
}
